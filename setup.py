from setuptools import setup, find_packages

setup(
    name="pplus",
    version="1.0.0",
    packages=find_packages(),
    install_requires=[], # No dependencies, because we're non-clucky!
    python_requires='>=3.6',
)
